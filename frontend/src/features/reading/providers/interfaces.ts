/**
 * Provider Interface Definitions
 *
 * Responsibilities:
 * - Define core interfaces for all swappable providers
 * - TTSProvider interface for text-to-speech services
 * - AudioPlayer interface for audio playback engines
 * - WordDetectionStrategy interface for highlighting logic
 * - Define capability discovery interfaces
 * - Specify common types (WordTiming, AudioChunk, etc.)
 * - Ensure consistent API across all implementations
 */
