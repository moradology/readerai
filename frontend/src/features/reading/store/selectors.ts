/**
 * Reading Feature Redux Selectors
 *
 * Responsibilities:
 * - Create memoized selectors for reading state
 * - Calculate derived state (progress percentage, time remaining)
 * - Select current word and surrounding context
 * - Provide filtered checkpoint/interruption data
 * - Optimize re-renders with proper memoization
 * - Export typed selector hooks for components
 */
