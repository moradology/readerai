/**
 * Reusable Button Component
 *
 * Responsibilities:
 * - Provide consistent button styling across app
 * - Support multiple variants (primary, secondary, ghost)
 * - Handle loading and disabled states
 * - Integrate with Tailwind CSS classes
 * - Support icons and different sizes
 * - Ensure accessibility (ARIA labels, keyboard navigation)
 * - Handle click events and prevent double-clicks
 */
