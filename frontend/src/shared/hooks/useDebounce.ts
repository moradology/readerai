/**
 * Debounce Hook
 *
 * Responsibilities:
 * - Debounce rapidly changing values
 * - Prevent excessive API calls or re-renders
 * - Support configurable delay
 * - Handle cleanup on unmount
 * - Maintain value stability
 * - Support immediate vs trailing execution
 * - Provide TypeScript generics for any value type
 */
