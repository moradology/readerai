/**
 * WebSocket Type Definitions
 *
 * Responsibilities:
 * - Define all WebSocket message types
 * - Specify connection state interfaces
 * - Type WebSocket events and handlers
 * - Define subscription callback types
 * - Ensure type safety for all messages
 * - Document message payload structures
 * - Define error types and codes
 */
