/**
 * Audio Service Interface Definitions
 *
 * Responsibilities:
 * - Define AudioPlayer interface for all implementations
 * - Specify audio streaming interfaces
 * - Define audio event types and callbacks
 * - Specify capability discovery interfaces
 * - Define audio state and metadata types
 * - Ensure consistent API across audio implementations
 * - Support both streaming and file-based playback
 */
